# pwd

> API designed to retrieve (`GET`) documents and texts from a VPS.

---

## Stack

- **Python** 3.13
- **FastAPI** — web framework
- **Uvicorn** — ASGI server
- **Ruff** — linter & formatter
- **Pytest** — testing
- **Poethepoet** — task runner (`poe`)
- **uv** — package & virtual environment manager

---

## Project structure

```
pwd/
├── .venv/                  # Virtual environment (managed by uv)
├── .zed/                   # Zed editor configuration
├── .gitignore
├── .python-version         # Python version in use (3.13)
├── main.py                 # Application entry point
├── pyproject.toml          # Project configuration and dependencies
├── uv.lock                 # Lockfile generated by uv
└── README.md
```

---

## Installation

### 1. Clone the project

```bash
git clone <repo-url> && cd pwd
```

### 2. Create the virtual environment

```bash
uv venv --python 3.13
```

### 3. Activate the virtual environment

```bash
source .venv/bin/activate
```

### 4. Install dependencies

```bash
uv sync
```

---

## Run the application

```bash
uv run uvicorn main:main --reload
```

---

## Available commands (via `poe`)

| Command             | Description                                        |
| ------------------- | -------------------------------------------------- |
| `uv run poe lint`   | Check the code with Ruff (without modifying files) |
| `uv run poe format` | Format the code with Ruff                          |
| `uv run poe fix`    | **Automatically fix** lint errors with Ruff        |

### Example — auto-fix the code

```bash
uv run poe fix
```

> This command runs `ruff check . --fix`, which analyses the entire project and **applies automatic fixes** (unsorted imports, E/F/I/W rules...).

---

## Tests

```bash
uv run pytest
```

---

## Ruff configuration (`pyproject.toml`)

| Parameter        | Value              |
| ---------------- | ------------------ |
| `target-version` | `py313`            |
| `line-length`    | `88`               |
| `indent-width`   | `4`                |
| `quote-style`    | `double`           |
| Enabled rules    | `E`, `F`, `I`, `W` |

```

The README has been fully translated to English. All sections — stack, project structure, installation steps, `poe` commands, and Ruff configuration — are now in English.
```
